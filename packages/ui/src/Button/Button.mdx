---
name: Button
route: /button
---

import { Playground, Props } from "docz"
import { Button } from "./"

# Button

## Props

<Props of={Button} />

## Basic usage

<Playground>
  <Button full={true} onClick={() => alert("Hello")}>Primary block</Button>
  <div style={{display: "flex"}}>
    <Button color="secondary" onClick={() => alert("Hello")}>
      Secondary block
    </Button>
  
    <Button color="tertiary" variant="outline" onClick={() => alert("Hello")}>
      Tertiary outline
    </Button>
  </div>

  <Button variant="outline" onClick={() => alert("Hello")}>Primary outline</Button>
  <Button variant="text" onClick={() => alert("Hello")}>Primary text</Button>
  <Button color="tertiary" variant="text" onClick={() => alert("Hello")}>Tertiary text</Button>
</Playground>

## Disabled

<Playground>
  <Button disabled={true} onClick={() => alert("Hello")}>Primary block</Button>
  <Button disabled={true} color="secondary" onClick={() => alert("Hello")}>
    Secondary block
  </Button>

  <Button disabled={true} full={true} variant="outline" onClick={() => alert("Hello")}>Primary outline</Button>
  <Button disabled={true} variant="text" onClick={() => alert("Hello")}>Primary text</Button>
  <Button disabled={true} color="tertiary" variant="text" onClick={() => alert("Hello")}>Primary text</Button>

</Playground>
