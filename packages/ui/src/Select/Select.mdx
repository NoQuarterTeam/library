---
name: Select
route: /select
---

import { Playground, Props } from "docz"
import { Select } from "./"
import Container from "../docz/Container"

# Select

## Basic usage

<Playground>
  <div style={{ width: "50%" }}>
    <Container
      initialState=""
      children={([state, setState]) => (
        <form
          onSubmit={e => {
            e.preventDefault()
          }}
        >
          <Select
            label="Email"
            value={state}
            required={true}
            onChange={e => setState(e.target.value)}
            options={["somit", "cool", "yeah"]}
          />
          <button type="submit" style={{ display: "none" }}>
            submit
          </button>
        </form>
      )}
    />
  </div>
  <div style={{ width: "50%" }}>
    <Container
      initialState=""
      children={([state, setState]) => (
        <Select
          label="Email with error"
          value={state}
          error={true}
          onChange={e => setState(e.target.value)}
          options={["somit", "cool", "yeah"]}
        />
      )}
    />
  </div>
</Playground>
