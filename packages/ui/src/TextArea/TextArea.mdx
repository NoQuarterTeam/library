---
name: TextArea
route: /TextArea
---

import { Playground, Props } from "docz"
import { TextArea } from "./"
import Container from "../docz/Container"

# TextArea

## Basic usage

<Playground>
  <div style={{ width: "50%" }}>
    <Container initialState="">
      {([value, setValue]) => (
        <TextArea
          label="Email"
          placeholder="heyyy"
          value={value}
          onChange={setValue}
          rows={4}
        />
      )}
    </Container>
    <TextArea error={true} label="Email" />
  </div>
</Playground>
